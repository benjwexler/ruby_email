<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @course.name %>
</p>

<p>
  <strong>Class hours:</strong>
  <%= @course.class_hours %>
</p>


<p>
  <strong>Cohorts:</strong>
  
  <%@course.cohorts.each do |cohort|%>
    <a href="/cohorts/<%=cohort.id%>"><%=cohort.start_date.strftime("%B, %d, %Y")%></a>,

    <td><a data-confirm="Are you sure?" data-remote="true" rel="nofollow" data-method="delete" href="/cohorts/<%=cohort.id%> ">Destroy</a></td>
    </div>  
    <br>

  <%end%>

 

</p>

<h1>New Cohort</h1>

<%= render 'new_cohort', cohort: @cohort %>

<%= link_to 'Edit', edit_course_path(@course) %> |
<%= link_to 'Back', courses_path %>
