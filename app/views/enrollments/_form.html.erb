<%= form_with(model: enrollment, local: true) do |form| %>
  <% if enrollment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(enrollment.errors.count, "error") %> prohibited this enrollment from being saved:</h2>

      <ul>
      <% enrollment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
  <%= form.label :student_id %><br>
  <%= form.collection_select :student_id, Student.all, :id, :full_name%>
</div>

  <div class="field">
  <%= form.label :cohort_id %><br>
  <%= form.collection_select :cohort_id, Cohort.all, :id, :name %>
</div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
